[目录](./)
# 通用业务代码规范

1. 所有可输入数据都必须做校验，且前后端都要做校验；
2. 所有带有开始、结束的时间参数，均已：`startTime`, `endTime` 来命名；
3. 所有通信接口全部以小写字母命名，如果遇到多个单词，以下划线区分；
4. 所有接口设计尽量符合 RESTFul 的要求；
5. 所有关于时间统一律采用毫秒级的时间戳，以 JS 为例是：`Date.getTime()` ；
6. 每个接口都要单独命名，遵循 `/模块/功能` 的形式，如：`/user/list`；
7. 前端不能处理过于复杂的业务逻辑；


## 功能规范

| 功能 | 代码 | 请求方式 | 备注 |
| --- | --- | --- | --- |
| 列表 | list | GET | |
| 编辑 | edit | POST / PUT | |
| 删除 | del | GET | |
| 添加 | add | POST | |